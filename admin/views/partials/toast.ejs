<%#
  partials/toast.ejs — toast stack mount point

  A single fixed container that /js/ui/toast.js fills with transient
  success / error / info messages (replaces native alert per Conventions
  rule #8). Rendered on every page, bare or shell.
-%>
<div id="toast-stack" class="toast-stack" role="status" aria-live="polite" aria-atomic="true"></div>
